[package]
name = "tokensaver-certification-confinement-linux"
version = "0.1.0"
description = "Trusted Linux namespace, seccomp, Landlock, and cgroup v2 certification driver"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
sha2.workspace = true
tokensaver-certification-confinement = { path = "../tsp-certification-confinement" }
tokensaver-certification-worker = { path = "../tsp-certification-worker" }
tsp-workbench = { path = "../tsp" }

[target.'cfg(target_os = "linux")'.dependencies]
libc.workspace = true
